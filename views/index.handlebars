<img src="{{imageLink}}" alt="{{alt}}" width="{{width}}"  height="{{height}}"/>
<h1>Eat-Da-Burger!</h1>

{{!-- Lists un-devoured Burgers --}}
<h2>Un-devoured Burgers!</h2>
<ol>
  {{#each burgers}}
  {{#unless devoured}}
  <li>
    {{burger_name}}
    <button class="change-devour" data-id="{{id}}" data-newdevour="true">
      Devour!
    </button>
  </li>

  {{/unless}}
  {{/each}}
</ol>
<hr>

<h2>Devoured Burgers!</h2>
<ol>
  {{#each burgers}}
  {{#if devoured}}
  <li class="change-devour" data-id="{{id}}" data-newdevour="false">
    {{burger_name}}
  </li>
  {{/if}}
  {{/each}}
</ol>